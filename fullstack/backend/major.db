CREATE DATABASE major;
USE major;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);
ALTER TABLE users 
ADD COLUMN role ENUM('recruiter', 'applicant') NOT NULL DEFAULT 'applicant';


SELECT * FROM users;
DESCRIBE users;
-- DROP TABLE users;
-- TRUNCATE TABLE users;